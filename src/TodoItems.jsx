import './todoitems.css'
export default function TodoItems(props) {
    // const { todoElements, deleteItem } = props
    // console.log(todoElements)
    return
    // <div>{todoElements.map(data => (
    //     <div className='todo' key={data?.id}>
    //         <p>{data?.item}</p>
    //         <button onSubmit={() => deleteItem(data.id)}>delete</button>
    //     </div>
    // ))}
    // </div>

}
